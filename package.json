{
  "name": "rrdelaney.github.io",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "chalk": "^2.0.1",
    "html-minifier": "^3.5.2",
    "prettier": "^1.5.3",
    "react-scripts": "^1.0.10"
  },
  "dependencies": {
    "dedent": "^0.7.0",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-emojione": "^3.1.10",
    "react-parallax": "^1.4.4",
    "styled-components": "^2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postbuild": "NODE_ENV=production babel-node --presets=env,react-app prerender.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "lint": "prettier --no-semi --single-quote -l \"src/**/*.js\"",
    "format": "prettier --no-semi --single-quote --write \"src/**/*.js\""
  }
}
